'use client';

// import FooterCarousel from "@/components/FooterCarousel";

export default function Footer() {
    return (
        <footer className={'position-fixed bottom-0 w-100 bg-dark'}>
            {/*<FooterCarousel/>*/}
        </footer>
    );
}